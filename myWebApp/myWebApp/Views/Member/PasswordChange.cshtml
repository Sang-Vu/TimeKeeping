
@{
    ViewBag.Title = Session["user"] + "Change Password";
    Layout = "~/Views/Shared/_Layout - Member.cshtml";
}

@using (Html.BeginForm("PasswordChange", "Member", FormMethod.Post))

{
<fieldset>
    <legend>ĐỔI MẬT KHẨU</legend>

    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
    <div class="form-outline">
        <label class="form-label" for="pass">Mật khẩu mới</label>
        <input type="password" class="form-control" name="newPassword" required />
    </div>
    <br />
    <input class="btn btn-success btn-block" type="submit" value="Đồng ý" />
    <!----<table>
        <tr>
            <td>
                Html.Label("Mật khẩu mới")
            </td>
            <td>
                Html.Password("newPassword")
            </td>
        </tr>
        <tr>
            <td></td>
            <td>
                <input type="submit" value="Đồng ý" />
            </td>

        </tr>
    </table>-->
</fieldset>
}




